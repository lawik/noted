{
  "repository": {},
  "description": " ",
  "license": "MIT",
  "scripts": {
    "deploy_old": "NODE_ENV=production webpack --mode production",
    "deploy": "NODE_ENV=production snowpack build",
    "watch_old": "webpack --mode development --watch",
    "watch": "NODE_ENV=development snowpack build --watch --no-minify --no-bundle"
  },
  "dependencies": {
    "alpinejs": "^2.8.0",
    "nprogress": "^0.2.0",
  },
  "devDependencies": {
    "@tailwindcss/jit": "^0.1.7",
    "autoprefixer": "^10.2.4",
    "snowpack": "^3.1.2",
    "tailwindcss": "^2.0.2"
  }
}
